"""Vendored pure Lefun protocol (framing, checksum, command IDs, parsers) from the
top-level lefun_ring.py (source of truth: ../../../lefun_ring.py). Stdlib-only, no bleak —
the coordinator supplies the BLE transport. Ported from Gadgetbridge's Lefun driver."""
